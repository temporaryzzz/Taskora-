package com.taskora.backend.utils;

import com.taskora.backend.entity.User;
import com.taskora.backend.service.UserService;

public class PasswordValidator {
    public boolean isPasswordValid(User user, UserService service, String password) {
        

        return true;
    }
}
