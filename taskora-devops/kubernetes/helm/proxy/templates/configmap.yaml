{{- if .Values.nginx.config }}
apiVersion: v1
kind: ConfigMap
metadata: 
  name: {{ include "proxy.fullname" . }}-nginx
  labels:
    {{- include "proxy.labels" . | nindent 4 }}
data:  
  default.conf: |
    {{- .Values.nginx.config | nindent 4 }}
{{- end }}